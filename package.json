{
  "type": "module",
  "private": true,
  "scripts": {
    "legacy:build": "node scripts/build.js",
    "build": "node scripts/build.js && nuxt generate && node scripts/copy-static.js",
    "dev": "node scripts/build.js && nuxt dev",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "exifr": "^7.1.3",
    "js-yaml": "^5.1.0"
  },
  "devDependencies": {
    "@lhci/cli": "^0.15.1",
    "highlight.js": "^11.9.0",
    "markdown-it": "^14.1.0",
    "nuxt": "^3.19.0",
    "sanitize-html": "^2.13.0"
  }
}

